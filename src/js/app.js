/* Vue JS */
var app = new Vue({
    el: '#vue',
    data: {
      message: 'VueJS: on.'
    }
})


/*
//Load a JSON file
var jsonData = $.getJSON('./src/data/somedata.json', function(data){
  doSomethingWith(data)
});
*/